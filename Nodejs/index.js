import { getPosts } from './postController.js'
// const {generateRandomNumber, celciusToFahrenheit} = require('./utilis')
// console.log(`Random number to generate: ${generateRandomNumber()}`)
// console.log(`Clcius to the fahrenheit: ${celciusToFahrenheit(60)}`)
console.log(getPosts())